{
  "name": "smc-remove-accents",
  "version": "1.0.0",
  "description": "Remove Vietnamese Accents",
  "main": "index.ts",
  "scripts": {
    "compile": "rm -rf dist/lib && tsc && tsc --build tsconfig.json"
  },
  "keywords": [],
  "author": "HienND23",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}